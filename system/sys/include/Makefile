DIST_TARGET_PATH := $(IMAGE)/system/include

all:

deps:

install:
	@cp -rp * $(DIST_TARGET_PATH)/
	rm -rf $(DIST_TARGET_PATH)/CVS
	rm $(DIST_TARGET_PATH)/Makefile

doc:

install-doc:

dist: install

clean:

