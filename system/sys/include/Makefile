DIST_TARGET_PATH := $(DIST_DIR)/atheos/sys/include

all:

deps:

install:
	@cp -rp * /system/include/
	rm -rf /system/include/CVS
	rm /system/include/Makefile

doc:

install-doc:

dist:
	@cp -rp * $(DIST_TARGET_PATH)/
	rm -rf $(DIST_TARGET_PATH)/CVS
	rm $(DIST_TARGET_PATH)/Makefile

clean:
