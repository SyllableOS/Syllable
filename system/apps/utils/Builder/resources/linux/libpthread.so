/* GNU ld script
   Use the shared library, but some functions are only in
   the static library, so try that secondarily.  */
OUTPUT_FORMAT(elf32-i386)
GROUP ( /system/resources/glibc/2.7/framework/libraries/libpthread.so.0 /system/development/resources/glibc/2.7/framework/libraries/libpthread_nonshared.a )
