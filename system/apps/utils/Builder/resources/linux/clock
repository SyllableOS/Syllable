# UTC=1 Hardware clock is set to UTC, also known as Greenwich time.
# UTC=0 Hardware clock is set to local time, usually the case on computers running Windows.
UTC=1

# Set this to any options you might need to give to the hwclock program,
# such as machine hardware clock type for Alpha computers.
CLOCKPARAMS=
