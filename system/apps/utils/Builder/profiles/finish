; Parts to complete a system build
; Build the finish-su profile before this!

resources
;	Make GLibC suitable for running in an installed system:
	system/resources/glibc/2.7/lib	libc.so

finish
	cp resources/grub/lib/grub/i386-pc/* boot/grub/ &&

	rm -r resources/documentation &&
	rm -r resources/documentation-DE &&
	rm -r resources/documentation-ES &&
	rm -r resources/documentation-NL &&
	rm -r resources/documentation-RU &&
	rm -r resources/documentation-SL
