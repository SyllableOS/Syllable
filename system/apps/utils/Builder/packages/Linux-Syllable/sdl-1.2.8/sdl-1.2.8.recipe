authors				Kaj de Vos
status				testing
notes				Seems to be for an older DirectFB

build-needs
	automake
	autoconf
;	Needed a Hermes fix:
;	gcc < 4.1
;	For DirectFB:
	pkg-config

configure-env		PKG_CONFIG_PATH "$PKG_CONFIG_PATH:/usr/directfb/lib/pkgconfig"
_configure-options
	--disable-video-fbcon
	--enable-video-directfb
