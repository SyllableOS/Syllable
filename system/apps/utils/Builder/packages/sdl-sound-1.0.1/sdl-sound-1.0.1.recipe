authors
	Flemming Sørensen
	Kaj de Vos
maintainers			Kaj de Vos
status				testing
warnings
	LibVorbis must be available in a stage subdirectory in the same parent directory as SDL-Sound.
notes				Doesn't build a shared library yet

licenses			LGPL
home				icculus.org/SDL_sound/
packages			SDL_sound-1.0.1
locations			http://icculus.org/SDL_sound/downloads/

needs				sdl >= 1.2
build-recommends
	libvorbis
	smpeg >= 0.4.3
	libflac
	libmodplug
	libmikmod >= 3.1.5
	libspeex

patch-config
;make-shared

configure-options
;	--enable-static
;	--disable-shared
;	--with-sdl-prefix=
;	--disable-sdltest
;	--with-vorbis-prefix=/usr/vorbis
	--with-vorbis-prefix=$PWD/../stage
;	--disable-vorbistest

documentation
	README COPYING CREDITS CHANGELOG TODO
;	docs

