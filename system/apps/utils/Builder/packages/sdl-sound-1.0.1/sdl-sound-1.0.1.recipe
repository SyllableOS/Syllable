authors
	Flemming Sørensen
	Kaj de Vos
maintainers			Kaj de Vos
status				testing
notes				Doesn't build a shared library yet

licenses			LGPL
home				icculus.org/SDL_sound/
packages			SDL_sound-1.0.1
locations			http://icculus.org/SDL_sound/downloads/

needs				sdl >= 1.2
build-needs			automake
build-recommends
	libvorbis
	smpeg >= 0.4.3
	libflac
	libmodplug
	libmikmod >= 3.1.5
	libspeex

patch-config
_patch
	aclocal
	automake

; Try to find LibOgg and LibVorbis.
; This assumes that SDL-Sound is built in a parent subdirectory, for example libs/sdl-sound-1.0.1:
make-env			LDFLAGS -L$PWD/../../stage/lib

documentation
	README COPYING CREDITS CHANGELOG TODO
;	docs
