authors				Kaj de Vos
status				testing
warnings
warnings			Builds upon SDL and others in the host system.

headline			SDL picture file loading library
description
	SDL_image is an image file loading library. It loads images as SDL surfaces, and supports the following formats: BMP, GIF, JPEG, LBM, PCX, PNG, PNM, TGA, TIFF, XCF, XPM, XV.
licenses			LGPL

home				www.libsdl.org/projects/SDL_image/
packages			SDL_image-1.2.8
locations			http://www.libsdl.org/projects/SDL_image/release/

needs				sdl >= 1.2.10
recommends
	LibPNG
	LibJPEG
	libtiff

;configure-options
;	--enable-tif
;	--enable-xcf

system-prefix-var	DESTDIR

documentation		README COPYING CHANGES
; For some packages:
;links				SDL/SDL_image.h framework/headers/
