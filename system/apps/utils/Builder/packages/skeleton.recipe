; Recipe information
authors				<Recipe and patches authors, in historical order>
maintainers			<Recipe maintainers, if not first author>
status				broken | incomplete | developing | testing
notes				<Internal notes>
;
; Package information
summary				<Brief headline>
description			<Full description>
licenses			GPL LGPL GFDL BSD MIT Apache Artistic Python MPL OS custom PD freeware commercial
;
home				syllable.sf.net
packages			<Deviating tarball name>
;locations			<Download locations>
;archives			<Arch archives>
;
; Dependencies (unimplemented):
;needs
;	<Build- and run-time Dependencies>
;build-needs
;	<Build-time dependencies>
;test-needs
;	<Test-time dependencies>
;run-needs
;	<Run-time dependencies>
;recommends
;	<Optional dependencies>
;
; Patching options:
patch-config		[<Directories>]
patch-legacy-config
patch				<Extra patching command sequence>
;
; Configuring:
; <configure>
; [<configure-vars>] <configure-cmd> [<configure-args>] [<configure-options>]
;
configure			<Configuration command sequence>
;
; Extra variables for configure:
configure-vars		VAR="value"
; Non-standard configure command:
configure-cmd		configure
; Non-standard prefix argument:
configure-args		--prefix=/usr/foo
; Extra configure options:
configure-options	--with-option
;
; Building:
; [<make-vars>] make <make-targets>
;
; Extra variables for make:
make-vars			VAR="value"
; Non-standard make targets:
;make-targets		all
;
; Testing:
; make <test-targets>
;
; Non-standard test targets:
test-targets		check
;
install-tree		<Extra subdirectories to create>
;
; Installing:
; <install>
; make [<install-prefix>] <install-targets>
;
install				<Installation command sequence>
;
; Non-standard install prefix option:
install-prefix		prefix=
; Non-standard install targets:
;install-targets		install
;
; Extra files to install:
documentation		README COPYING ABOUT-NLS NEWS ChangeLog TODO AUTHORS THANKS
; Links to create:
	<Destination>
		<Source>
; Extra files to pack:
distro-files		/bin/link
