authors				Kaj de Vos
status				testing
warnings			Bootstraps demo upon itself in the host system.

inherits			gnu

headline			Character terminal emulation library
description
	The Ncurses (new curses) library is a free software emulation of curses in System V Release 4.0, and more. It uses Terminfo format, supports pads and color and multiple highlights and forms characters and function-key mapping, and has all the other SYSV-curses enhancements over BSD Curses.
; ?:
licenses			MIT

home				www.gnu.org/software/ncurses/
packages			ncurses/ncurses-5.7

build-needs
	gawk
	diffutils
conflicts			termcap

; Breaks if not using a subdirectory:
no-includedir
no-datarootdir
;configure-options	--includedir=/resources/NCurses/framework/headers/ncursesw

system-prefix-var	DESTDIR

documentation
	README INSTALL ANNOUNCE NEWS AUTHORS TO-DO
	doc/html/
develop-docs		doc/ncurses-intro.doc
internal-docs		doc/hackguide.doc

pre-register
	echo 'INPUT(-lncursesw)'	> framework/libraries/libncurses.so	&&
;	echo 'INPUT(-lcursesw)'		> framework/libraries/libcurses.so	&&
	echo 'INPUT(-lformw)'		> framework/libraries/libform.so	&&
	echo 'INPUT(-lpanelw)'		> framework/libraries/libpanel.so	&&
	echo 'INPUT(-lmenuw)'		> framework/libraries/libmenu.so	&&
	echo 'INPUT(-lncursesw)'	> framework/libraries/libcursesw.so

;move-files			data/terminfo/ /usr/share/
