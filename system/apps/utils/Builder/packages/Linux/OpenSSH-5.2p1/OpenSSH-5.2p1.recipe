authors				Kaj de Vos
status				testing

configure-options
	--sysconfdir=/etc/ssh
;	--datadir=/usr/share/sshd
	--with-privsep-path=/var/lib/sshd
	--with-md5-passwords
;	--with-pam
;	--with-xauth=/usr/bin/xauth
;	--with-ssl-dir=/resources/OpenSSL
;	--with-zlib=/system/indexes
;	--with-libs='-lcrypt'
;	Doesn't work as advertised:
;	--disable-libutil

; For LFS boot scripts:
links				sbin/sshd /usr/sbin/
