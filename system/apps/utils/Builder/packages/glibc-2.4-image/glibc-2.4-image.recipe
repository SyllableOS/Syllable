authors				Kaj de Vos
status				testing
notes
	Make the GLibC linker script function in the system stage, to avoid compiling it twice

inherits			glibc-2.4

pre-register
	echo 'OUTPUT_FORMAT(elf32-i386)' > lib/libc.so
	echo "GROUP ( $IMAGE/system/libraries/libc.so.2 $IMAGE/usr/glibc/lib/libc_nonshared.a )" >> lib/libc.so
