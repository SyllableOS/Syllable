authors
	Rui Caridade
	Kaj de Vos
status				testing
notes				Hangs during compilation on some machines

; To find SDL_net:
build-needs			Syllable > 0.6.4

; We need an extra register:
configure-env
	CFLAGS "$CFLAGS -fomit-frame-pointer"
	CXXFLAGS "$CXXFLAGS -fomit-frame-pointer"
_configure-options
;	--disable-dynamic-x86
	--disable-opengl
