authors				Kaj de Vos
status				testing

build-needs			Syllable >= 0.6.6

configure-options
	-des
	-Dmksymlinks
	-Accflags='-march=i586 -pipe'
	-Aoptimize='-O2'
;	-Dinstallprefix=
;	-Dnoextensions=
	-Duseshrplib
	-Dccflags='-DPERL_EXTERNAL_GLOB'
;	-Dlibpth=$PWD
;	-Uusemymalloc
;	-Dusethreads
;	-Duseperlio -Dusesfio

documentation
	README Artistic Copying AUTHORS
;	May be too huge:
	Changes
links
	perl5.10.0								programs/perl
	5.10.0/i586-syllable/CORE/libperl.so	framework/libraries/
;	Handled by the system build:
;	For OpenSSL and others:
;	programs/perl							/usr/bin/perl
;	For OpenSSL:
;	programs/perl							/usr/local/bin/perl
