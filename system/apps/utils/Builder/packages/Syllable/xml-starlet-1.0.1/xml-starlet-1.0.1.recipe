authors				Kaj de Vos
status				testing
warnings
	Source tarball contains hard link!
	Move the source to the correct (package) name manually, run build patch, then continue building.

patch-config

configure-options
	--with-libxml-prefix=/resources/libxml2
	--with-libxslt-prefix=/resources/libxslt
;	--disable-ipv6
