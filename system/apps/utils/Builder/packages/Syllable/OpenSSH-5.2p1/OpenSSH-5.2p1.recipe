authors				Kaj de Vos
status				testing

no-sysconfdir
configure-options
	--sysconfdir=/etc/ssh
;	--datadir=/usr/share/sshd
	--with-privsep-path=/var/lib/sshd
;	--with-md5-passwords
;	--with-ssl-dir=/system/indexes/OpenSSL
;	--with-zlib=/system/indexes/zlib
;	Needed to build:
	--with-libs='-lcrypt'
;	Doesn't work as advertised:
;	--disable-libutil
