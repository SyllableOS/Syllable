authors
	Flemming Sørensen
	Kaj de Vos
maintainers			Kaj de Vos
status				testing

make-shared

configure-env
;	For Syllable 0.6.6:
	IMAGE			''
;	For LibPNG and LibTIFF configure tests:
	LDFLAGS			"$LDFLAGS -lz -lm -ljpeg"

links
;	For some packages:
	SDL/SDL_image.h			include/
;	For compatibility (with version 1.2.4):
	libSDL_image-1.2.so.0	lib/libSDL_image-1.2.so
