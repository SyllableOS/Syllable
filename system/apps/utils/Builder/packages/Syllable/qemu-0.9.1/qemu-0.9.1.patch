diff -ru qemu-0.9.1-original/audio/ossaudio.c qemu-0.9.1/audio/ossaudio.c
--- qemu-0.9.1-original/audio/ossaudio.c	2008-01-06 20:38:41.000000000 +0100
+++ qemu-0.9.1/audio/ossaudio.c	2008-01-27 18:16:38.000000000 +0100
@@ -25,11 +25,15 @@
 #include <sys/mman.h>
 #include <sys/types.h>
 #include <sys/ioctl.h>
+
 #ifdef __OpenBSD__
-#include <soundcard.h>
+	#include <soundcard.h>
+#elif defined(__SYLLABLE__)
+	#include <atheos/soundcard.h>
 #else
-#include <sys/soundcard.h>
+	#include <sys/soundcard.h>
 #endif
+
 #include "qemu-common.h"
 #include "audio.h"
 
diff -ru qemu-0.9.1-original/fpu/softfloat.h qemu-0.9.1/fpu/softfloat.h
--- qemu-0.9.1-original/fpu/softfloat.h	2008-01-06 20:38:42.000000000 +0100
+++ qemu-0.9.1/fpu/softfloat.h	2008-01-27 18:16:38.000000000 +0100
@@ -47,6 +47,7 @@
 | implementations of C, `flag', `uint8', and `int8' should all be `typedef'ed
 | to the same as `int'.
 *----------------------------------------------------------------------------*/
+#ifndef __SYLLABLE__
 typedef uint8_t flag;
 typedef uint8_t uint8;
 typedef int8_t int8;
@@ -56,7 +57,7 @@
 typedef signed int int32;
 typedef uint64_t uint64;
 typedef int64_t int64;
-
+#endif
 /*----------------------------------------------------------------------------
 | Each of the following `typedef's defines a type that holds integers
 | of _exactly_ the number of bits specified.  For instance, for most
diff -ru qemu-0.9.1-original/Makefile.target qemu-0.9.1/Makefile.target
--- qemu-0.9.1-original/Makefile.target	2008-01-06 20:38:41.000000000 +0100
+++ qemu-0.9.1/Makefile.target	2008-01-27 18:16:38.000000000 +0100
@@ -97,7 +97,7 @@
 endif
 
 # We require -O2 to avoid the stack setup prologue in EXIT_TB
-OP_CFLAGS := -Wall -O2 -g -fno-strict-aliasing
+OP_CFLAGS := -Wall -O2 -g -fno-strict-aliasing -fno-pic
 
 # cc-option
 # Usage: OP_CFLAGS+=$(call cc-option, -falign-functions=0, -malign-functions=0)
diff -ru qemu-0.9.1-original/vl.c qemu-0.9.1/vl.c
--- qemu-0.9.1-original/vl.c	2008-01-06 20:38:42.000000000 +0100
+++ qemu-0.9.1/vl.c	2008-01-27 18:16:38.000000000 +0100
@@ -67,6 +67,10 @@
 #elif defined (__GLIBC__) && defined (__FreeBSD_kernel__)
 #include <freebsd/stdlib.h>
 #else
+#ifdef __SYLLABLE__
+#include <net/if.h>
+#include <net/if_tun.h>
+#else
 #ifndef __sun__
 #include <linux/if.h>
 #include <linux/if_tun.h>
@@ -101,6 +105,7 @@
 #include <winsock2.h>
 int inet_aton(const char *cp, struct in_addr *ia);
 #endif
+#endif
 
 #if defined(CONFIG_SLIRP)
 #include "libslirp.h"
@@ -4070,6 +4075,12 @@
     fcntl(fd, F_SETFL, O_NONBLOCK);
     return fd;
 }
+#elif defined(__SYLLABLE__)
+static int tap_open(char *ifname, int ifname_size)
+{
+    fprintf(stderr, "warning: tap_open not yet implemented\n");
+    return -1;
+}
 #else
 static int tap_open(char *ifname, int ifname_size)
 {
