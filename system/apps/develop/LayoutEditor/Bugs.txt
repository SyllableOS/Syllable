ImageButton needs Invalidate() after SetImage()
Menu needs Invalidate() after changing the menu layout and calling InvalidateLayout()
Spinner should use the dynamic layout system
Slider does not call os::Control method in AttachedToWindow()
Slider has no GetMinMax() method
Slider has no way to set the knob mode after the object has been constructed
Slider should have a SetOrientation() method
PopupMenu should have os::Control as its base class
PopupMenu needs a way to set the menu after construction
ListView needs a way to change the columns

